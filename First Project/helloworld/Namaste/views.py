from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.

def HomePageViewNew(request):
    return HttpResponse("Hello World My First Project In Django We Framework")
    return HttpResponse("Hello Means Namaste in Hindi Language")
